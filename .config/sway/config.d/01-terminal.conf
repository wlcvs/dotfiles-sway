# Terminal padrão
set $term alacritty

unbindsym $mod+Return
bindsym $mod+Return exec alacritty

# Rofi: apenas apps com desktop entry (sem executáveis do PATH)
unbindsym $mod+d
bindsym $mod+d exec sh -c 'pkill -x rofi || rofi -show drun'
