# Default terminal
set $term alacritty

unbindsym $mod+Return
bindsym $mod+Return exec alacritty

# Rofi: only .desktop entries (no raw PATH executables)
unbindsym $mod+d
bindsym $mod+d exec sh -c 'pkill -x rofi || rofi -show drun -theme ~/.config/rofi/theme.rasi'
