# Print: select area and copy to clipboard
bindsym {
    Print             exec grimshot --notify copy area
    Alt+Print         exec grimshot --notify copy active
    Ctrl+Print        exec grimshot --notify copy output
    Shift+Print       exec grimshot --notify save output
}
